#include <bits/stdc++.h>
using namespace std;
#define fast ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
#define ip cin>>
#define op cout<<
#define int long long
signed main(){
    fast
    int n, k, x;
    ip n >> k >> x;
    vector<int> a(n);

    for(auto &A: a) {
        ip A;
    }

    sort(a.begin(), a.end());

    for(int i = 0; i < n; i++) {
        
    }
}