#include <bits/stdc++.h>
using namespace std;
#define fast ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
#define ip cin>>
#define op cout<<
#define endl "\n" 
#define int long long
signed main(){
    fast
    int t, n;
    cin>>t;
    while(t--){
        int m;
        ip n >> m;
        vector<int> a(n);
        for(int i = 0; i < n; i++) {
            ip a[i];
        }

        vector<string> s(n);
        for(int i = 0; i < n; i++) {
            ip s[i];
        }

        
    }
}